apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-devops-cyber-assessment
spec:
  selector:
    app: devops-cyber-assessment
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
